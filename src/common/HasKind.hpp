#ifndef VERWALTUNGSSCHALE_referencing_HPP_HasKind
#define VERWALTUNGSSCHALE_referencing_HPP_HasKind

namespace Verwaltungsschale {
    namespace common {
        class HasKind {
            types::Kind kind = types::Kind::Type;
        };
    }
}

#endif
